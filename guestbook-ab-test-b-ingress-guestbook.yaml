apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: guestbook
  namespace: guestbook-ab-test-b
spec:
  rules:
  - host: guestbook-ab-test-b.local.amazinglyabstract.it
    http:
      paths:
      - backend:
          service:
            name: guestbook
            port:
              number: 80
        path: /
        pathType: Prefix
