apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: guestbook
  namespace: guestbook-ab-test-a
spec:
  rules:
  - host: guestbook-ab-test-a.local.amazinglyabstract.it
    http:
      paths:
      - backend:
          service:
            name: guestbook
            port:
              number: 80
        path: /
        pathType: Prefix
